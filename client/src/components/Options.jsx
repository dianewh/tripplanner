import React from 'react';
import styles from '../css/Options.css';

class Options extends React.Component {
  constructor(props) {
    super(props);
  }

  render () {
    return (
      <div>
        Display options to chooose here
      </div>
    );
  }
}

export default Options;